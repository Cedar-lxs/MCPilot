# MCPilot 项目开发记录

**日期：** 2026-06-30
**项目路径：** D:\MCPilot

---

## 对话摘要

### 背景
大哥在开发 MCPilot 项目，基于 MCP 协议的一套 Agent 工具集。

项目架构（`项目架构.md`）：
```
src/
├── mcp_server/       ← MCP 服务端
│   └── tools/        ← 各种工具
├── mcp_client/       ← MCP 客户端
├── agent/            ← Agent 核心
├── rag/              ← RAG 引擎
├── api/              ← FastAPI 接口
└── utils/            ← 工具函数
```

### 已完成的工具（大哥昨天自己写的）
1. **`note.py`** — `NoteTool` 临时笔记（write/read/list/delete）
2. **`calculator.py`** — `Calculator` 安全数学计算器（有笔误：`excute` 非 `execute`）
3. **`web_serach.py`** — `WebSearchTool` 网络搜索（模拟数据，文件名有笔误 `serach`）

### 今天的进展
1. 发现项目在 `D:\MCPilot`
2. 阅读并理解了三个工具的代码结构（都有 `get_definition()` 和 `execute()`）
3. 安装了 `mcp==1.28.1`（MCP Python SDK）
4. 在 `src/mcp_server/tools/server.py` 创建了 MCP Server 框架
5. 测试验证：3 个工具均可正常发现和执行

### 当前状态
- `server.py` 已经清空，准备引导大哥自己写
- 已讲完：项目根目录加到 `sys.path`、MCP SDK import
- 正在引导阶段：Step 2 — 创建 Server 实例和两个核心接口

### 大哥的要求
- 要引导式教学，不要直接写完整代码
- 对话内容要备份到 D 盘

---

## 项目关键信息

### 日志工具
- `utils/logger_handler.py` 提供 `logger = get_logger()` 模块级实例
- 工具通过 `from utils.logger_handler import logger` 引用

### 工具类规范
- 必须有 `get_definition()` → 返回 dict（name, description, inputSchema）
- 必须有 `execute(**kwargs)` → 返回字符串

### 工具文件
```
D:\MCPilot\src\mcp_server\tools\
├── __init__.py     ← 空
├── calculator.py
├── note.py
├── server.py       ← 正在开发中（已清空）
└── web_serach.py
```

### Python 环境
- Python 3.13.9
- mcp==1.28.1, uvicorn==0.49.0
- 已安装：fastapi, openai, pydantic 等（见 requirements.txt）

---
